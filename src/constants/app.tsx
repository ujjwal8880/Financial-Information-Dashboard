import { blueGrey, green, red } from '@mui/material/colors'

export const dataMaxLength = 110

export const statusColorMapping = {
	active: green['600'],
	blocked: red['600'],
	closed: blueGrey['600'],
}
